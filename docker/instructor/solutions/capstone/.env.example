# Copy to .env before running:  cp .env.example .env
#
# R17. This file is committed; .env is not. It documents which variables exist
# without leaking their values.

POSTGRES_USER=app
POSTGRES_PASSWORD=change-me-in-real-life
POSTGRES_DB=app

HTTP_PORT=8080
